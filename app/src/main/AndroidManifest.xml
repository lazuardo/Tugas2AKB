<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugas2akb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Tugas 2 AKB"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:theme="@style/Theme2" android:name=".HomeActivity"></activity>
        <activity android:theme="@style/Theme1" android:name=".VerifyACC" />
        <activity android:theme="@style/Theme1" android:name=".AlmostActivity"  />
        <activity android:theme="@style/Theme2" android:name=".RegisterActivity"  />
        <activity android:theme="@style/Theme1" android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>